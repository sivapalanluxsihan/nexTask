{
  "name": "nextask-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "nexTask - Collaborative Task Management System Monorepo",
  "scripts": {
    "dev": "pnpm --parallel run dev",
    "build": "pnpm --recursive run build",
    "prod": "pnpm run build && pnpm --parallel run start",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md,css,html}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md,css,html}\"",
    "lint": "pnpm --recursive run lint",
    "typecheck": "pnpm --recursive run typecheck",
    "seed": "pnpm --filter @nextask/server seed"
  },
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^6.0.2",
    "prettier": "^3.8.4",
    "typescript": "^6.0.3"
  }
}
